import React from 'react';
import Layout from './../../../components/AppLayout/index';

const AppLayout = ({ ...props }) => {
    return (
        <Layout {...props}>
        </Layout>
    );
};

export default AppLayout;